import { createLab } from 'vue-design-lab'
import './styles/tokens.css'

// The workbench shell, auto routing over src/pages/, and the token pipeline
// all come from the vue-design-lab package; this file is the entire host app.
createLab().mount('#app')
