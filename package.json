{
  "name": "create-vue-design-lab",
  "version": "0.1.0",
  "type": "module",
  "description": "Scaffold a Vue-flavored design lab where designers learn HTML/CSS/Vue by typesetting real poster materials.",
  "//": "root devDependencies exist only so host-template/ resolves types in-editor and in vue-tsc; they never ship (devDeps are excluded from npm pack)",
  "license": "MIT",
  "bin": {
    "create-vue-design-lab": "index.mjs"
  },
  "files": [
    "index.mjs",
    "host-template"
  ],
  "keywords": [
    "vue",
    "create",
    "scaffold",
    "design",
    "material",
    "poster",
    "typesetting",
    "vibe-coding"
  ],
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.24.0",
  "scripts": {
    "build": "pnpm -C template build",
    "prepublishOnly": "pnpm -C template build"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.0",
    "vue-design-lab": "workspace:*"
  }
}
